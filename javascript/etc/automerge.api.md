## API Report File for "@automerge/automerge"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public (undocumented)
export type ActorId = string;

// @public
export function addCommits<T>(doc: Doc<T>, commits: Commit[], opts?: ApplyOptions<T>): Doc<T>;

// @public
export function addFragments<T>(doc: Doc<T>, fragments: Fragment[], opts?: ApplyOptions<T>): Doc<T>;

// @public
export function anonymize<T>(doc: Doc<T>): Doc<Record<string, unknown>>;

// Warning: (ae-forgotten-export) The symbol "API_2" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type API = API_2;

// @public
export function applyChanges<T>(doc: Doc<T>, changes: Change[], opts?: ApplyOptions<T>): [Doc<T>];

// @public
export type ApplyOptions<T> = {
    patchCallback?: PatchCallback<T>;
};

// @public (undocumented)
export function applyPatch(doc: unknown, patch: Patch): void;

// @public (undocumented)
export function applyPatches(doc: unknown, patches: Patch[]): void;

// @public (undocumented)
export type AutomergeValue = ScalarValue | {
    [key: string]: AutomergeValue;
} | Array<AutomergeValue>;

// @public
export function block<T>(doc: Doc<T>, path: Prop[], index: number | Cursor): {
    [key: string]: MaterializeValue;
} | null;

// @public
export function bundleFragmentMetadata(doc: Doc<unknown>, fragments: FragmentMeta[]): Uint8Array[];

// @public (undocumented)
export type Change = Uint8Array;

// @public
export function change<T>(doc: Doc<T>, options: string | ChangeOptions<T> | ChangeFn<T>, callback?: ChangeFn<T>): Doc<T>;

// @public
export function changeAt<T>(doc: Doc<T>, scope: Heads, options: string | ChangeOptions<T> | ChangeFn<T>, callback?: ChangeFn<T>): ChangeAtResult<T>;

// @public
export type ChangeAtResult<T> = {
    newDoc: Doc<T>;
    newHeads: Heads | null;
};

// @public
export type ChangeFn<T> = (doc: T) => void;

// @public (undocumented)
export type ChangeMetadata = {
    actor: ActorId;
    author: Author | null;
    seq: number;
    startOp: number;
    maxOp: number;
    time: number;
    message: string | null;
    deps: Heads;
    hash: Hash;
    extraBytes: string | null;
};

// @public
export type ChangeOptions<T> = {
    message?: string;
    time?: number | undefined;
    patchCallback?: PatchCallback<T>;
};

// @public
export function clone<T>(doc: Doc<T>, _opts?: ActorId | InitOptions<T>): Doc<T>;

// @public (undocumented)
export type Commit = {
    head: Hash;
    parents: Heads;
    bytes: Uint8Array;
};

// @public
export type Conflicts = {
    [key: string]: AutomergeValue;
};

// @public
export class Counter {
    constructor(value?: number);
    decrement(_delta: number): number;
    increment(_delta: number): number;
    toJSON(): number;
    toString(): string;
    // (undocumented)
    value: number;
    valueOf(): number;
}

// @public (undocumented)
export type Cursor = string;

// @public (undocumented)
export function decodeChange(data: Change): DecodedChange;

// @public (undocumented)
export type DecodedChange = {
    actor: ActorId;
    author: Author | null;
    seq: number;
    startOp: number;
    time: number;
    message: string | null;
    deps: Heads;
    hash: Hash;
    ops: Op[];
};

// @public (undocumented)
export type DecodedSyncMessage = {
    heads: Heads;
    need: Heads;
    have: SyncHave[];
    changes: Change[];
};

// @public (undocumented)
export function decodeSyncMessage(message: SyncMessage): DecodedSyncMessage;

// @public
export function decodeSyncState(state: Uint8Array): SyncState;

// @public
export function deleteAt<T>(list: T[], index: number, numDelete?: number): void;

// @public (undocumented)
export type DelPatch = {
    action: "del";
    path: Prop[];
    length?: number;
};

// @public
export function diff(doc: Doc<unknown>, before: Heads, after: Heads): Patch[];

// @public (undocumented)
export type DiffOptions = {
    recursive?: boolean;
};

// @public
export function diffPath(doc: Doc<unknown>, path: Prop[], before: Heads, after: Heads, opts?: DiffOptions): Patch[];

// @public
export type Doc<T> = {
    readonly [P in keyof T]: T[P];
};

// @public (undocumented)
export function dump<T>(doc: Doc<T>): void;

// @public
export function emptyChange<T>(doc: Doc<T>, options: string | ChangeOptions<T> | void): Doc<T>;

// Warning: (ae-forgotten-export) The symbol "ChangeToEncode" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function encodeChange(change: ChangeToEncode): Change;

// @public (undocumented)
export function encodeSyncMessage(message: DecodedSyncMessage): SyncMessage;

// @public
export function encodeSyncState(state: SyncState): Uint8Array;

// @public (undocumented)
export function equals(val1: unknown, val2: unknown): boolean;

// @public (undocumented)
export class Float64 {
    constructor(value: number);
    // (undocumented)
    value: number;
}

// @public (undocumented)
export type Fragment = FragmentMeta & {
    bytes: Uint8Array;
};

// @public (undocumented)
export type FragmentLevelRange = number | { start?: number; end?: number } | null | undefined;

// @public (undocumented)
export type FragmentMeta = {
    head: Hash;
    level: number;
    boundary: Heads;
    checkpoints: Heads;
    members: Heads;
};

// @public
export function free<T>(doc: Doc<T>): void;

// @public
export function from<T extends Record<string, unknown>>(initialState: T | Doc<T>, _opts?: ActorId | InitOptions<T>): Doc<T>;

// @public
export function generateSyncMessage<T>(doc: Doc<T>, inState: SyncState): [SyncState, SyncMessage | null];

// @public
export function getActorId<T>(doc: Doc<T>): ActorId;

// @public (undocumented)
export function getActorsForAuthor<T>(doc: Doc<T>, author: Author): ActorId[];

// @public
export function getAllChanges<T>(doc: Doc<T>): Change[];

// @public (undocumented)
export function getAuthor<T>(doc: Doc<T>): Author | null;

// @public (undocumented)
export function getAuthorForActor<T>(doc: Doc<T>, actor: ActorId): Author | null;

// @public (undocumented)
export function getAuthors<T>(doc: Doc<T>): Author[];

// Warning: (ae-forgotten-export) The symbol "Automerge" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function getBackend<T>(doc: Doc<T>): Automerge;

// @public
export function getChanges<T>(oldState: Doc<T>, newState: Doc<T>): Change[];

// @public (undocumented)
export function getChangesMetaSince<T>(state: Doc<T>, heads: Heads): ChangeMetadata[];

// @public (undocumented)
export function getChangesSince<T>(state: Doc<T>, heads: Heads): Change[];

// @public
export function getCommits(doc: Doc<unknown>): Commit[];

// @public
export function getConflicts<T>(doc: Doc<T>, prop: Prop): Conflicts | undefined;

// Warning: (ae-forgotten-export) The symbol "CursorPosition" needs to be exported by the entry point index.d.ts
//
// @public
export function getCursor<T>(doc: Doc<T>, path: Prop[], position: CursorPosition, move?: MoveCursor): Cursor;

// @public
export function getCursorPosition<T>(doc: Doc<T>, path: Prop[], cursor: Cursor): number;

// @public
export function getFragmentMeta(doc: Doc<unknown>, head: string): FragmentMeta | null;

// @public
export function getFragmentMetadata(doc: Doc<unknown>, levels?: FragmentLevelRange): FragmentMeta[];

// @public
export function getFragments(doc: Doc<unknown>, levels?: FragmentLevelRange): Fragment[];

// @public
export function getHeads<T>(doc: Doc<T>): Heads;

// @public (undocumented)
export function getHistory<T>(doc: Doc<T>): State<T>[];

// @public
export function getLastLocalChange<T>(doc: Doc<T>): Change | undefined;

// @public
export function getMissingDeps<T>(doc: Doc<T>, heads: Heads): Heads;

// @public
export function getObjectId(doc: any, prop?: Prop): ObjID | null;

// @public
export function hasHeads(doc: Doc<unknown>, heads: Heads): boolean;

// @public
export function hasOurChanges<T>(doc: Doc<T>, remoteState: SyncState): boolean;

// @public (undocumented)
export type Heads = Hash[];

// @public (undocumented)
export class ImmutableString {
    // (undocumented)
    [IMMUTABLE_STRING]: boolean;
    constructor(val: string);
    // (undocumented)
    toJSON(): string;
    toString(): string;
    // (undocumented)
    val: string;
}

// @public (undocumented)
export type IncPatch = {
    action: "inc";
    path: Prop[];
    value: number;
};

// @public
export function init<T>(_opts?: ActorId | InitOptions<T>): Doc<T>;

// @public
export function initializeBase64Wasm(wasmBase64: string): Promise<void>;

// @public
export function initializeWasm(wasmBlob: Uint8Array | Request | Promise<Uint8Array> | string): Promise<void>;

// @public
export type InitOptions<T> = {
    actor?: ActorId;
    author?: Author;
    freeze?: boolean;
    patchCallback?: PatchCallback<T>;
    unchecked?: boolean;
    allowMissingChanges?: boolean;
    convertImmutableStringsToText?: boolean;
};

// @public
export function initSyncState(options?: {
    readOnly?: boolean;
}): SyncState;

// @public
export function insertAt<T>(list: T[], index: number, ...values: T[]): void;

// @public (undocumented)
export type InsertPatch = {
    action: "insert";
    path: Prop[];
    values: PatchValue[];
    marks?: MarkSet;
    conflicts?: boolean[];
};

// @public
export function inspectChange(doc: Doc<unknown>, changeHash: string): DecodedChange | null;

// @public (undocumented)
export class Int {
    constructor(value: number);
    // (undocumented)
    value: number;
}

// @public (undocumented)
export function isAutomerge(doc: unknown): boolean;

// @public (undocumented)
export function isCounter(obj: any): obj is Counter;

// @public (undocumented)
export function isImmutableString(obj: any): obj is ImmutableString;

// @public @deprecated (undocumented)
export const isRawString: typeof isImmutableString;

// @public
export function isWasmInitialized(): boolean;

// @public
export function joinBlock<T>(doc: Doc<T>, path: Prop[], index: number | Cursor): void;

// @public (undocumented)
export type JsReleaseInfo = {
    gitHead: string;
};

// @public
export interface List<T> extends Array<T> {
    // (undocumented)
    deleteAt(index: number, numDelete?: number): List<T>;
    // (undocumented)
    insertAt(index: number, ...args: T[]): List<T>;
}

// @public
export function load<T>(data: Uint8Array, _opts?: ActorId | InitOptions<T>): Doc<T>;

// @public
export function loadIncremental<T>(doc: Doc<T>, data: Uint8Array, opts?: ApplyOptions<T>): Doc<T>;

// @public (undocumented)
export type Mark = {
    name: string;
    value: ScalarValue_2;
    start: number;
    end: number;
};

// @public (undocumented)
export function mark<T>(doc: Doc<T>, path: Prop[], range: MarkRange, name: string, value: MarkValue): void;

// @public (undocumented)
export type MarkRange = {
    expand?: "before" | "after" | "both" | "none";
    start: number;
    end: number;
};

// @public (undocumented)
export function marks<T>(doc: Doc<T>, path: Prop[]): Mark[];

// @public (undocumented)
export function marksAt<T>(doc: Doc<T>, path: Prop[], index: number): MarkSet;

// @public (undocumented)
export interface MarkSet {
    // (undocumented)
    [name: string]: ScalarValue_2;
}

// @public (undocumented)
export type MarkValue = string | number | null | boolean | Date | Uint8Array;

// Warning: (ae-forgotten-export) The symbol "Value" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type MaterializeValue =
| { [key: string]: MaterializeValue }
| Array<MaterializeValue>
| Value;

// @public
export function merge<T>(local: Doc<T>, remote: Doc<T>): Doc<T>;

// @public (undocumented)
export type MoveCursor = "before" | "after";

declare namespace next {
    export {
        insertAt,
        deleteAt,
        use,
        getBackend,
        getChangesSince,
        getChangesMetaSince,
        init,
        view,
        clone,
        anonymize,
        free,
        from,
        change,
        changeAt,
        emptyChange,
        load,
        loadIncremental,
        saveIncremental,
        save,
        merge,
        getActorId,
        getAuthor,
        getAuthors,
        getAuthorForActor,
        getActorsForAuthor,
        getConflicts,
        getLastLocalChange,
        getObjectId,
        getChanges,
        getAllChanges,
        applyChanges,
        getHistory,
        diff,
        diffPath,
        equals,
        encodeSyncState,
        decodeSyncState,
        generateSyncMessage,
        receiveSyncMessage,
        hasOurChanges,
        initSyncState,
        encodeChange,
        decodeChange,
        encodeSyncMessage,
        decodeSyncMessage,
        getMissingDeps,
        getHeads,
        dump,
        toJS,
        isAutomerge,
        saveSince,
        hasHeads,
        topoHistoryTraversal,
        inspectChange,
        stats,
        releaseInfo,
        splice,
        updateText,
        spans,
        block,
        splitBlock,
        joinBlock,
        updateBlock,
        updateSpans,
        getCursor,
        getCursorPosition,
        mark,
        unmark,
        marks,
        marksAt,
        saveBundle,
        getFragmentMetadata,
        getFragmentMeta,
        bundleFragmentMetadata,
        getCommits,
        getFragments,
        addCommits,
        addFragments,
        readBundle,
        isImmutableString,
        isCounter,
        AutomergeValue,
        Cursor,
        Counter,
        Doc,
        Int,
        Uint,
        Float64,
        Mark,
        MarkSet,
        MarkRange,
        MarkValue,
        MoveCursor,
        Patch,
        PatchCallback,
        ScalarValue,
        PatchInfo,
        PatchSource,
        ChangeMetadata,
        FragmentMeta,
        Commit,
        Fragment,
        FragmentLevelRange,
        PutPatch,
        DelPatch,
        DiffOptions,
        SpliceTextPatch,
        InsertPatch,
        IncPatch,
        Span,
        SyncMessage,
        UpdateSpansConfig,
        initializeWasm,
        initializeBase64Wasm,
        wasmInitialized,
        isWasmInitialized,
        ImmutableString,
        applyPatch,
        applyPatches,
        ChangeOptions,
        ApplyOptions,
        List,
        ChangeFn,
        State,
        InitOptions,
        ChangeAtResult,
        API,
        SyncState,
        ActorId,
        Conflicts,
        Prop,
        Change,
        ObjID,
        DecodedChange,
        DecodedSyncMessage,
        Heads,
        MaterializeValue,
        JsReleaseInfo,
        WasmReleaseInfo,
        ReleaseInfo,
        isRawString,
        RawString
    }
}

// @public (undocumented)
export type ObjID = string;

// Warning: (ae-forgotten-export) The symbol "MarkPatch" needs to be exported by the entry point index.d.ts
// Warning: (ae-forgotten-export) The symbol "UnmarkPatch" needs to be exported by the entry point index.d.ts
// Warning: (ae-forgotten-export) The symbol "ConflictPatch" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type Patch =
| PutPatch
| DelPatch
| SpliceTextPatch
| IncPatch
| InsertPatch
| MarkPatch
| UnmarkPatch
| ConflictPatch;

// @public
export type PatchCallback<T> = (patches: Array<Patch>, info: PatchInfo<T>) => void;

// @public (undocumented)
export type PatchInfo<T> = {
    before: Doc<T>;
    after: Doc<T>;
    source: PatchSource;
};

// @public (undocumented)
export type PatchSource = "from" | "emptyChange" | "change" | "changeAt" | "merge" | "loadIncremental" | "applyChanges" | "addCommits" | "addFragments" | "receiveSyncMessage";

// @public (undocumented)
export type Prop = string | number;

// @public (undocumented)
export type PutPatch = {
    action: "put";
    path: Prop[];
    value: PatchValue;
    conflict?: boolean;
};

// @public @deprecated (undocumented)
export const RawString: typeof ImmutableString;

// @public
export function readBundle(bundle: Uint8Array): {
    changes: DecodedChange[];
    deps: Heads;
};

// @public
export function receiveSyncMessage<T>(doc: Doc<T>, inState: SyncState, message: SyncMessage, opts?: ApplyOptions<T>): [Doc<T>, SyncState, null];

// @public (undocumented)
export type ReleaseInfo = {
    js: JsReleaseInfo;
    wasm: WasmReleaseInfo | null;
};

// @public
export function releaseInfo(): ReleaseInfo;

// @public
export function save<T>(doc: Doc<T>): Uint8Array;

// @public
export function saveBundle(doc: Doc<unknown>, hashes: string[]): Uint8Array;

// @public
export function saveIncremental<T>(doc: Doc<T>): Uint8Array;

// @public (undocumented)
export function saveSince(doc: Doc<unknown>, heads: Heads): Uint8Array;

// @public (undocumented)
export type ScalarValue = string | number | null | boolean | Date | Counter | Uint8Array | ImmutableString;

// @public (undocumented)
export type Span =
| { type: "text"; value: string; marks?: MarkSet }
| { type: "block"; value: { [key: string]: MaterializeValue } };

// @public
export function spans<T>(doc: Doc<T>, path: Prop[]): Span[];

// @public
export function splice<T>(doc: Doc<T>, path: Prop[], index: number | Cursor, del: number, newText?: string): void;

// @public (undocumented)
export type SpliceTextPatch = {
    action: "splice";
    path: Prop[];
    value: string;
    marks?: MarkSet;
};

// @public
export function splitBlock<T>(doc: Doc<T>, path: Prop[], index: number | Cursor, block: {
    [key: string]: MaterializeValue;
}): void;

// @public (undocumented)
export interface State<T> {
    // (undocumented)
    change: DecodedChange;
    // (undocumented)
    snapshot: T;
}

// @public
export function stats(doc: Doc<unknown>): {
    numChanges: number;
    numOps: number;
    numActors: number;
    cargoPackageName: string;
    cargoPackageVersion: string;
    rustcVersion: string;
};

// @public (undocumented)
export type SyncMessage = Uint8Array;

// Warning: (ae-forgotten-export) The symbol "JsSyncState" needs to be exported by the entry point index.d.ts
//
// @public
export type SyncState = JsSyncState & {
    _opaque: typeof _SyncStateSymbol;
};

// @public (undocumented)
export function toJS<T>(doc: Doc<T>): T;

// @public (undocumented)
export function topoHistoryTraversal(doc: Doc<unknown>): string[];

// @public (undocumented)
export class Uint {
    constructor(value: number);
    // (undocumented)
    value: number;
}

// @public (undocumented)
export function unmark<T>(doc: Doc<T>, path: Prop[], range: MarkRange, name: string): void;

// @public
export function updateBlock<T>(doc: Doc<T>, path: Prop[], index: number | Cursor, block: {
    [key: string]: MaterializeValue;
}): void;

// @public
export function updateSpans<T>(doc: Doc<T>, path: Prop[], newSpans: Span[], config?: UpdateSpansConfig): void;

// @public (undocumented)
export type UpdateSpansConfig = {
    defaultExpand?: "before" | "after" | "both" | "none";
    perMarkExpand?: {[key: string]: "before" | "after" | "both" | "none" }
}

// @beta
export function updateText(doc: Doc<unknown>, path: Prop[], newText: string): void;

// @public (undocumented)
export function use(api: API): void;

// @public
export function view<T>(doc: Doc<T>, heads: Heads): Doc<T>;

// @public
export function wasmInitialized(): Promise<void>;

// @public (undocumented)
export type WasmReleaseInfo = {
    gitHead: string;
    cargoPackageName: string;
    cargoPackageVersion: string;
    rustcVersion: string;
};

// Warnings were encountered during analysis:
//
// dist/wasm_types.d.ts:89:5 - (ae-forgotten-export) The symbol "SyncHave" needs to be exported by the entry point index.d.ts
// dist/wasm_types.d.ts:102:5 - (ae-forgotten-export) The symbol "Op" needs to be exported by the entry point index.d.ts
// dist/wasm_types.d.ts:107:5 - (ae-forgotten-export) The symbol "Author" needs to be exported by the entry point index.d.ts
// dist/wasm_types.d.ts:114:5 - (ae-forgotten-export) The symbol "Hash" needs to be exported by the entry point index.d.ts
// dist/wasm_types.d.ts:220:5 - (ae-forgotten-export) The symbol "PatchValue" needs to be exported by the entry point index.d.ts
// dist/wasm_types.d.ts:232:5 - (ae-forgotten-export) The symbol "ScalarValue_2" needs to be exported by the entry point index.d.ts

// (No @packageDocumentation comment for this package)

```
