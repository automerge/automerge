# Progress

- 16:41 UTC: read brief/common plan/spec/revised B packet and Rust TDD skill. Verified clean assigned jj workspace, pinned parent and Rust 1.90. Baseline test result is coordinator-supplied, not independently rerun. Wrote bounded local plan before implementation.
- First red `red-01.log`: missing private EX-01 API (compile failure). `green-01.log`: EX-01 authority checkpoints green with native action 8, real integrated operations and original suggestion identity.
- `behavior-02.log`: 8/9 tests passed, including all 1,920 EX-01 schedules with independent intermediate oracle, duplicates and real patch replay; EX-07 FAILED because restored attachment omitted preexisting child. `green-07.log`: structural-existence predicate for exposure fixes the failing replay.
- `red-status.log` -> `green-status.log`: evidence received before its control must still publish inspection transitions. Captures now retain complete immutable policy facts; policy-only identity is distinct from equal content heads.
- `red-context.log` -> `green-context.log`: fresh context pending before grant was incorrectly classified excluded by active R. Fixed independent context decision; both EX-03 tests pass.
- `red-authoring-api.log`, then semantic `red-authoring-behavior.log` -> `green-authoring.log`: selected deletion targets original A; transaction-local operations visible; unobserved continuing actor history isolated; object-local edits into excluded but structurally observed map now allowed. Direct-target EX-05/06 pass. No general list/counter/formatting claims.
- Native gate red API -> green strict group test (`red-gate.log`, `green-native.log`); rejected valid-prefix/malformed-tail groups and bad ancestry without publication. Empty-key predecessors, unknown-target late arrival, control-only identity and compact save/load pass.
- Gate-disabled initial test attempted to construct action 8 through an expanded builder and panicked during validation (`gate-disabled.log`); replaced that test setup with real feature-enabled bytes. `gate-disabled-characterized.log` proves standalone change/compact rejection and ordinary incremental valid-prefix acceptance. No universal old-reader fail-closed claim.
- `red-export.log` -> `green-map-checkpoint.log`: complete in-memory package reconstruction retains queued content, pending evidence, non-prefix selection, and rejects missing bytes. `red-inspection.log` -> `green-inspection.log`: explicit dependency manifests and provenance-bound capture reconstruction.
- `ex02-schedules.log`: 192 schedules pass. `ex03-schedules.log`: 1,920 schedules pass, each published group checks independent fixture states, real patch replay, and frozen rereads.
- `red-unsupported.log` -> `green-unsupported.log`: private session now rejects unproven structural catalogue and competing controls instead of exposing an accidentally broad API. Native bytes remain native, not a map audit field.
- Pure evaluator extracted to `prototype/policy.rs` (`refactor-core.log` green); named Clock fields replace tuple internals. `red-observer.log` -> `green-observer.log`: stale/foreign patch contexts rejected atomically. `model-registers.log`: 48 generated valid direct-predecessor chains, arbitrary context holes, reconstruction and reinstatement pass.
- `native-routes.log`: native batched apply/import patches, merge, incremental change bytes, compact reconstruction retain control identity and content noninterference. These are allow-all transport characterizations, NOT selected optimized incremental-log proof. Content-only save/load demonstrably loses authority interpretation.
- Self-review found unknown capture heads were silently ignored by baseline clocks: `red-capture-heads.log` -> `green-capture-heads.log` now rejects them before editing.
- Final targeted rustfmt passes (`rustfmt-final.log`). Final full feature suite: **499 passed, 1 ignored** (`full-feature-final.log`); default suite: **473 passed, 1 ignored** (`full-default-final.log`). Only the two known manifest warnings. Source unchanged after these full runs except documentation.

